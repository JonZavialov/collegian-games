[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# API proxy for CFB stats
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
